interface:
  display_name: "Patristic Index Extractor"
  short_description: "Extract structured patrística volume indexes reliably"
  default_prompt: "Use $patristic-index-extractor to inspect one PG, PL, or PO volume, apply the collection-specific taxonomy, and write the parsed index structure to SQLite."

policy:
  allow_implicit_invocation: false
